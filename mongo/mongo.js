function createNewDB() {
    try {
        //Create the DB here
    } catch(e) {
        //Handle error here
        throw(e);
        //to let the invoker know of the error and handle exit gracefully
    }

}